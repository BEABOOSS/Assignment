Module TestFunc
    Sub Main()
        'Dim db As New DbManager()
        'db.TestConnection()


        'For Each row As DataRow In db.GetAllUser()
        '    Console.WriteLine($"Id: {row("user_ID")}, username: {row("username")}, gender: { GenderExension.GetDisplayName(row("gender"))}")
        'Next
        'Dim res As DialogResult = MessageBox.Show("text to be displayed", "title bar", MessageBoxButtons.OKCancel, MessageBoxIcon.Question, MessageBoxDefaultButton.Button2)
        'Console.WriteLine(res.ToString)
        Console.ReadKey()

    End Sub



End Module



